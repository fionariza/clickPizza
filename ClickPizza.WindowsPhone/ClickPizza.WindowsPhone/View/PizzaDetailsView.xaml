<UserControl x:Class="ClickPizza.WindowsPhone.View.PizzaDetailsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="480" d:DesignWidth="480">
    
    <StackPanel x:Name="LayoutRoot" Background="{StaticResource PhoneChromeBrush}">
        <Image Source="{Binding Path=ImageUri}" HorizontalAlignment="Left"  Height="100" Width="300" VerticalAlignment="Top" />
        <TextBlock TextWrapping="Wrap" Text="{Binding Path=Name}"/>
        <TextBlock TextWrapping="Wrap" Text="{Binding Path=Composition}"/>
        <TextBlock TextWrapping="Wrap" Text="{Binding Path=Length}"/>
        <TextBlock TextWrapping="Wrap" Text="{Binding Path = Weight}"/>
        <TextBlock TextWrapping="Wrap" Text="{Binding Path = Price}"/>
    </StackPanel>
</UserControl>